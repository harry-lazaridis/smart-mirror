{
  "name": "smart-mirror",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "firebase": "^12.12.0",
    "react": "^19.2.5"
  }
}
